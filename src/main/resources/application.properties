# Jay Shree Textiles E-Commerce Backend

# Server Configuration
server.port=${PORT:8080}

# Application Name
spring.application.name=whitehouse-backend

# Firebase Configuration
firebase.project-id=e-com-tex
# firebase.service-account=classpath:firebase-service-account.json

# Stripe Configuration
stripe.api.key=${STRIPE_API_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# Clerk Configuration
clerk.api.key=${CLERK_API_KEY}
clerk.frontend.api=${CLERK_FRONTEND_API}

# CORS Configuration
cors.allowed-origins=${FRONTEND_URL:http://localhost:5173,http://localhost:5174}

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email Configuration (Gmail SMTP with SSL)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=sugus7215@gmail.com
spring.mail.password=uhelvgmpswkyejpe
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.properties.mail.debug=true

# Store name for email templates
app.store.name=Jay Shree Textiles
app.store.url=http://localhost:5173
app.admin.email=sugus7215@gmail.com

logging.file.name=application.log
logging.level.root=INFO
